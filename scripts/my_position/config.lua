Config = {}

-- Default spawn location for new players
Config.DefaultSpawn = {
    x = -515.5311,
    y = -254.5744,
    z = 35.6268,
    heading = 0.0
}

-- How often client sends position updates to server (milliseconds)
Config.UpdateInterval = 5000

-- Delay before teleporting player to saved position (milliseconds)
Config.SpawnDelay = 2000

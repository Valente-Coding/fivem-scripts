-- lbg-char-master shared config

CharConfig = {
    -- Set to true to auto-open character creator if no saved data exists
    AutoOpenOnFirstJoin = true,
    -- Command to open the character editor
    EditCommand = "charedit"
}
